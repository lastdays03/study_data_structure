# 4주차: 정렬, 시각화 및 최종 프로젝트

## 📌 학습 목표
- 다양한 정렬 알고리즘의 동작 원리와 시간 복잡도를 이해하고 구현합니다.
- 알고리즘의 동작 과정을 눈으로 볼 수 있는 시각화 프로그램을 만들어봅니다.
- 지금까지 배운 자료구조를 라이브러리화하여 최종 프로젝트를 완성합니다.

## 📚 핵심 학습 내용 (Ch 13 ~ Ch 24)

### 1. 정렬 알고리즘 (Sorting) (Ch 13)
- **기본 정렬 (O(n^2))**:
  - **버블 정렬 (Bubble Sort)**: 인접한 두 원소를 비교 교환
  - **선택 정렬 (Selection Sort)**: 최솟값을 찾아 맨 앞과 교환
  - **삽입 정렬 (Insertion Sort)**: 정렬된 부분에 새 원소를 적절한 위치에 삽입
- **고급 정렬 (O(n log n))**:
  - **병합 정렬 (Merge Sort)**: 분할 정복(Divide and Conquer) 사용. 안정 정렬.
  - **퀵 정렬 (Quick Sort)**: 피벗(Pivot)을 기준으로 분할. 평균적으로 가장 빠름.
  - **힙 정렬 (Heap Sort)**: 힙 자료구조 활용.

### 2. 알고리즘 시각화 (Visualization) (Ch 16)
- **이벤트 반응형 프로그래밍**: 사용자의 입력(클릭, 키보드)에 반응하는 구조
- **Tkinter**: 파이썬 내장 GUI 라이브러리 활용
- 정렬 과정이나 트리/그래프의 탐색 과정을 애니메이션으로 표현

### 3. 최종 프로젝트 (Ch 24)
- 나만의 자료구조 라이브러리 구축
- 학습한 내용 총정리 및 회고

## 💻 실습 과제

### 과제 1: 정렬 알고리즘 성능 비교
- 버블, 선택, 삽입, 퀵, 병합 정렬을 모두 구현하세요.
- 랜덤한 숫자 10,000개를 생성하여 각 알고리즘의 정렬 소요 시간을 측정하고 비교해보세요 (`import time` 모듈 활용).

### 과제 2: 정렬 시각화 (선택 사항)
- `matplotlib` (설치 필요: `pip install matplotlib`) 또는 `tkinter` (내장 모듈)를 사용하여 막대 그래프가 정렬되는 과정을 시각화해보세요.
- 값이 교환될 때마다 그래프를 갱신하여 정렬 과정을 눈으로 확인합니다.

### 과제 3: 나만의 자료구조 라이브러리 완성
- 1~3주차에 구현한 `LinkedList`, `Stack`, `Queue`, `BST`, `Graph` 클래스들을 하나의 패키지나 모듈로 정리하세요.
- 각 클래스에 대한 문서화(Docstring)와 단위 테스트(Unit Test)를 작성하여 완성도를 높이세요.

## ✅ 체크리스트
- [ ] O(n^2) 정렬과 O(n log n) 정렬의 차이를 명확히 이해했는가?
- [ ] 퀵 정렬의 최악의 경우(O(n^2))는 언제 발생하는가?
- [ ] Stable Sort(안정 정렬)와 Unstable Sort(불안정 정렬)의 의미를 아는가?
