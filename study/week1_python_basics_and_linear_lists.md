# 1주차: 파이썬 기초 및 선형 자료구조

## 📌 학습 목표
- 파이썬의 변수와 객체 참조 방식(Call by Object Reference)을 깊이 이해합니다.
- 선형 자료구조의 대표격인 연결 리스트(Linked List)를 직접 구현하며 포인터 개념을 익힙니다.
- 스택(Stack)과 큐(Queue)의 동작 원리를 이해하고 구현합니다.

## 📚 핵심 학습 내용 (Ch 00 ~ Ch 06)

### 1. 파이썬 변수는 객체에 붙인 이름표다 (Ch 01)
- **변수와 객체**: 파이썬에서 변수는 값을 저장하는 상자가 아니라, 객체를 가리키는 '이름표'입니다.
- **참조(Reference)**: 변수에 값을 할당하면 실제로는 객체의 메모리 주소를 참조하게 됩니다.
- **`id()` 함수**: 객체의 고유 주소(Identity)를 확인하여 참조 관계를 파악할 수 있습니다.
- **Mutable vs Immutable**: 리스트(Mutable)와 튜플/문자열(Immutable)의 차이점을 이해합니다.

### 2. 연결 리스트 (Linked List) (Ch 03)
- **구조**: 데이터(Data)와 다음 노드를 가리키는 링크(Link)로 구성된 노드들의 집합입니다.
- **특징**:
  - 메모리에 연속적으로 저장되지 않아도 됩니다.
  - 삽입/삭제가 배열보다 효율적일 수 있습니다 (위치를 아는 경우 O(1)).
  - 임의 접근(Random Access)이 불가능하여 탐색 속도는 느립니다 (O(n)).
- **구현**:
  - `Node` 클래스: `data`와 `next` 속성
  - `LinkedList` 클래스: `head` 노드 관리, `append`, `delete`, `print_all` 메서드 구현

### 3. 스택 (Stack)
- **LIFO (Last In First Out)**: 나중에 들어온 데이터가 먼저 나가는 구조입니다.
- **활용**: 함수 호출 스택, 뒤로 가기, 괄호 검사 등
- **구현**: 파이썬의 `list`를 사용하여 `append()`(push)와 `pop()`으로 간단히 구현 가능합니다.

### 4. 큐 (Queue)
- **FIFO (First In First Out)**: 먼저 들어온 데이터가 먼저 나가는 구조입니다.
- **활용**: 작업 대기열, 프린터 스풀러, BFS 등
- **구현**: 파이썬의 `collections.deque`를 사용하면 효율적입니다 (`append()`, `popleft()`).

## 💻 실습 과제

### 과제 1: 파이썬 참조 확인하기
- 정수형 변수 `a = 10`, `b = 10`을 선언하고 `id(a)`와 `id(b)`가 같은지 확인해보세요.
- 리스트 `l1 = [1, 2]`, `l2 = [1, 2]`를 선언하고 `id(l1)`과 `id(l2)`가 같은지 확인해보세요.
- 얕은 복사(`copy()`)와 깊은 복사(`deepcopy()`)의 차이를 코드로 확인해보세요.

### 과제 2: 단일 연결 리스트 구현 (Singly Linked List)
- `Node` 클래스와 `LinkedList` 클래스를 직접 구현하세요.
- 다음 메서드를 포함해야 합니다:
  - `append(data)`: 리스트 끝에 노드 추가
  - `get_node(index)`: 특정 인덱스의 노드 반환
  - `add_node(index, data)`: 특정 인덱스에 노드 추가
  - `delete_node(index)`: 특정 인덱스의 노드 삭제
  - `print_all()`: 모든 노드의 데이터 출력

### 과제 3: 스택과 큐 구현
- 리스트를 사용하지 않고, 위에서 구현한 `LinkedList`를 활용하여 `Stack`과 `Queue` 클래스를 구현해보세요.
- `Stack`: `push(data)`, `pop()`, `peek()`, `is_empty()`
- `Queue`: `enqueue(data)`, `dequeue()`, `peek()`, `is_empty()`

## ✅ 체크리스트
- [ ] 파이썬의 변수 할당 과정(참조)을 그림으로 그릴 수 있는가?
- [ ] 연결 리스트의 삽입/삭제 과정에서 링크 연결 순서를 정확히 이해했는가?
- [ ] 스택과 큐의 차이점을 설명하고 적절한 활용 사례를 들 수 있는가?
